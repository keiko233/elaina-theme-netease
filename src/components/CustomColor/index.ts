import { initLS } from "../../utils/localStorage";

export const customColorStyleStatus = ref(initLS('elaina-customColorStyleStatus', false));

export const customColor = ref(initLS('elaina-customColor', '#ff1958'));
